package org.RepositorySearch;

/**
 * Searchs through github. The json content is transformed before and then put
 * into database.
 * Later on it can be searched with the sqlite json extension
 */
public class GHQueryRepository{

    

}
