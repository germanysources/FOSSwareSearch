package org.RepositorySearch;

/**
 * This interface execute the query at the hosting provider
 * and puts the results in an memory database to filter it later with sql queries
 */
public interface QueryRepository{

    

}
